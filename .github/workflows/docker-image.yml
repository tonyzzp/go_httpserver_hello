name: build docker image

on:
  push:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: checkut
      uses: actions/checkout@v3
    - name: Build the Docker image
      run: |
       docker build -t ${{vars.DOCKER_IMAGE_NAME}} .
       docker login -u tonyzzp -p ${{secrets.DOCKER_USER_PASSWORD}}
       docker push ${{vars.DOCKER_IMAGE_NAME}}
       
